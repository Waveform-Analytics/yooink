""" base_request.py """

import requests
from typing import Any, Dict, List, Union


def get_url_contents(
        url, api_username, api_token
) -> Union[Dict[str, Any], List[Any]]:
    """

    Args:
        url:
        api_username:
        api_token:

    Returns:

    """
